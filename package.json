{
  "devDependencies": {
    "@types/node": "^18"
  },
  "dependencies": {
    "@pulumi/cloudflare": "^5.12.0",
    "@pulumi/pulumi": "^3.86.0",
    "typescript": "^4.0.0"
  },
  "name": "${PROJECT}",
  "description": "The main files are:",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "desteves",
  "license": "ISC"
}
